struct Bar {
	function: () -> int;
	field: int;
};

struct Foo {
	thing: Bar;
};

test: () -> int {
	
}

main: () -> void {
	
	object: Foo;
	
	object.thing.field = 0;
	object.thing.function = test;

	while (object.thing.field < 1500000) {
		object.thing.field += 1;
	}
	
}
