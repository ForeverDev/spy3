print: cfunc (format: ^byte, ...) -> void;

factorial: (n: int) -> int {
	if (n <= 1) 
		return 1;
	return n * factorial(n - 1);
}

main: () -> void {

	n: int;

	n = 10;
	
	print("factorial of %d is %d\n", n, factorial(n));
}

